export const metadata = {
  title: "Blog | Portfolio",
  description: "Writing and thoughts on software engineering and architecture.",
}

export default function BlogLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
