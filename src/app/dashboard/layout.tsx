export const metadata = {
  title: "Dashboard | Portfolio",
  description: "Real-time analytics and portfolio metrics.",
}

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
